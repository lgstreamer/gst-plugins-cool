if USE_PLAYREADY
PLAYREADY_DIR=playready
else
PLAYREADY_DIR=
endif

if USE_WIDEVINE
WIDEVINE_DIR=widevine
else
WIDEVINE_DIR=
endif

SUBDIRS = marlin $(PLAYREADY_DIR) uhdcp $(WIDEVINE_DIR)
DIS_SUBDIRS = marlin $(PLAYREADY_DIR) uhdcp $(WIDEVINE_DIR)
